export const enum EBackupMaterial {
    Mnemonic = 'mnemonic',
    Xpub = 'xpub',
    Xprv = 'xprv',
    Descriptor = 'descriptor',
}

export const enum ENet {
    Bitcoin = 'bitcoin',
    Testnet = 'testnet',
}
