export const enum BackupMaterial {
    Mnemonic = 'mnemonic',
    Xpub = 'xpub',
    Xprv = 'xprv',
    Descriptor = 'descriptor',
}

export const enum Net {
    Bitcoin = 'bitcoin',
    Testnet = 'testnet',
}
